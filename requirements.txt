# Core Scientific Computing
numpy
pandas
scipy
matplotlib
colormaps  # Note: not on conda-forge, installed via pip in Colab

# Geospatial/GIS Libraries
# Note: In Colab, gdal/rasterio/pyproj/shapely/geopandas are installed via conda
# to avoid compilation issues. The notebook setup automatically excludes these
# from pip installation when running in Colab.
rasterio
pyproj
gdal
geopandas
shapely
rioxarray

# Point Cloud Processing
# Note: In Colab, pdal is installed as 'python-pdal' via conda to avoid build errors.
# small_gicp is also handled via conda. Both are excluded from pip in Colab.
pdal
small_gicp

# Performance Optimization
numba

# Web and Data Access
requests

# Interactive Jupyter Widgets
ipyleaflet
ipywidgets

# Configuration
pyyaml
